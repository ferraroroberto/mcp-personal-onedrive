# Model Context Protocol SDK (Python)
mcp>=1.2.0

# Microsoft Authentication Library — handles MSAL /consumers PKCE flow + token cache
msal>=1.28.0

# HTTP client for Microsoft Graph
httpx>=0.27.0

# Environment variable loader
python-dotenv>=1.0.0

# JWT signing for the OAuth gateway (HS256)
PyJWT>=2.8.0

# Web stack used by FastMCP's Streamable HTTP transport. Pinning explicitly so
# we can mount our own OAuth routes on the same Starlette app.
starlette>=0.36.0
uvicorn[standard]>=0.27.0
